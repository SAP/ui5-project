{
	"$schema": "http://json-schema.org/draft-07/schema",
	"$id": "http://ui5.sap/schema/specVersion/3.0.json",

	"type": "object",
	"required": ["specVersion"],
	"properties": {
		"specVersion": { "enum": ["3.0"] },
		"kind": {
			"enum": ["project", "extension", null],
			"$comment": "Using null to allow not defining 'kind' which defaults to project"
		}
	},
	"if": {
		"properties": {
			"kind": {
				"enum": ["project", null],
				"$comment": "Using null to allow not defining 'kind' which defaults to project"
			}
		}
	},
	"then": {
		"$ref": "3.0/kind/project.json"
	},
	"else": {
		"if": {
			"properties": {
				"kind": {
					"enum": ["extension"]
				}
			}
		},
		"then": {
			"$ref": "3.0/kind/extension.json"
		}
	}
}
