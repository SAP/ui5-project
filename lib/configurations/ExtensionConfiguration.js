const AbstractConfiguration = require("./AbstractConfiguration");
const {validate} = require("../validation/validator");

class ExtensionConfiguration extends AbstractConfiguration {
	async validate() {

	}
}

module.exports = ExtensionConfiguration;
